/**
 * @04-agent-teams/shared
 *
 * Shared business logic for 04-agent-teams.
 *
 * Import specific modules via subpath exports:
 *   import { PromaAgent, type AgentEvent } from '@04-agent-teams/shared/agent';
 *
 * Available modules:
 *   - agent: PromaAgent SDK wrapper, event handling, tool matching
 */

// Note: Import via subpath exports (e.g., '@04-agent-teams/shared/agent')
// See package.json exports field for available subpaths
